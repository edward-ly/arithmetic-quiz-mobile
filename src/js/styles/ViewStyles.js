import { StyleSheet } from "react-native";

export default ViewStyles = StyleSheet.create({
  mainPageContainer: {
    backgroundColor: "#fff",
    paddingTop: 24,
    paddingRight: 12,
    paddingBottom: 12,
    paddingLeft: 12,
  },
  modalContainer: {
    backgroundColor: "#fff",
    borderRadius: 8,
    padding: 16,
  },
});